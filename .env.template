# AIClient-2-API 环境变量配置文件
# 复制此文件为 .env 并修改相应配置

# ===========================================
# OAuth 回调配置（必须）
# ===========================================
# 公网IP或域名，用于OAuth回调URL
# 示例：OAUTH_HOST=123.456.789.123 或 OAUTH_HOST=yourdomain.com
OAUTH_HOST=your-public-ip-or-domain

# ===========================================
# 服务器配置
# ===========================================
# 主服务端口
PORT=3000

# 运行环境
NODE_ENV=production

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info

# ===========================================
# 认证配置（可选）
# ===========================================
# Web UI API密钥（如果需要）
# REQUIRED_API_KEY=your-secure-api-key

# 登录过期时间（秒，默认3600秒=1小时）
# LOGIN_EXPIRY=3600

# ===========================================
# 代理配置（可选）
# ===========================================
# HTTP代理URL
# PROXY_URL=http://127.0.0.1:1089

# 启用代理的提供商（逗号分隔）
# PROXY_ENABLED_PROVIDERS=gemini-cli-oauth,gemini-antigravity

# ===========================================
# 高级配置（可选）
# ===========================================
# 主进程管理端口
# MASTER_PORT=3100

# 工作进程标识
# IS_WORKER_PROCESS=false

# OpenAI推理最大token数
# OPENAI_REASONING_MAX_TOKENS=50000

# Codex指令注入开关
# CODEX_INSTRUCTIONS_ENABLED=true